# ============================================================================
# RENDER DEPLOYMENT - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy these environment variables to Render Dashboard:
# Settings → Environment Variables
# ============================================================================

# Database (Render PostgreSQL)
# Formato: postgresql://username:password@hostname:port/database
# Exemplo:
# DATABASE_URL=postgresql://cmms_user:cmms_password@dpg-xxx.render.internal/cmms_enterprise
DATABASE_URL=postgresql://cmms_user:cmms_password@localhost/cmms_enterprise

# Redis (opcional - remova se não usar)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Server
PORT=3000
NODE_ENV=production

# JWT - DEVE SER MUDADO EM PRODUCÇÃO!
JWT_SECRET=your-super-secure-secret-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secure-refresh-key-change-this-in-production
JWT_EXPIRY=1h
JWT_REFRESH_EXPIRY=7d

# CORS - DEVE SER MUDADO PARA O DOMINIO DO RENDER!
# Exemplo: https://seu-app-name.onrender.com
CORS_ORIGIN=http://localhost:5173

# Admin user (usado no seed inicial)
ADMIN_EMAIL=admin@cmms.com
ADMIN_PASSWORD=Admin@123456

# Logging
LOG_LEVEL=info

# ============================================================================
# INSTRUÇÕES PARA RENDER
# ============================================================================
#
# 1. Render Dashboard → Seu Serviço → Settings
#
# 2. Aqui embaixo no Environment Variables, adicione:
#    - DATABASE_URL: postgresql://...
#    - JWT_SECRET: gere um secret seguro
#    - JWT_REFRESH_SECRET: gere outro secret seguro
#    - CORS_ORIGIN: https://seu-app.onrender.com
#    - NODE_ENV: production
#
# 3. Valores que NO RENDER são gerados automaticamente:
#    DATABASE_URL: Vem da database PostgreSQL criada no Render
#    PORT: Render usa 10000 por padrão (ignore PORT=3000)
#
# 4. Para gerar secrets seguros (terminal):
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 5. Deploy:
#    - Push para main branch do GitHub
#    - Render detecta automaticamente
#    - Deploy inicia
#
# ============================================================================

# Se está usando Render PostgreSQL:
# - Vá para Dashboard → Data (PostgreSQL)
# - Copie a CONNECTION STRING (Internal Database URL)
# - Coloque em DATABASE_URL acima

# Exemplo: postgresql://cmms_user:xxxxxxxxxxxx@dpg-abc123.render.internal/cmms_enterprise
